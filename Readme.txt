bu-bu.xlsx -- is a sample excel file contains other language name(bulgarian language used as sample)

sample_template.json -- this is the template file for English. 

The above two files are input files. we will compare these files and create new output.json file at runtime.

output.json file is a template file for Bulgaria. 